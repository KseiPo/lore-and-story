/// The `lore/` slice: lore model, entity-tree walk, convention matcher (pure),
/// and the browse/editor/preview UI.
///
/// Placeholder for Epic 2 — the Dart loader port (Story 2.1a) lands here and
/// depends inward on the `storage/` slice's [RepoStorage] port.
library;
