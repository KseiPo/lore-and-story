---
scope: Lore & Story mobile app (Flutter/Dart)
purpose: Build substrate: invariants keeping JS reference + Dart port + shells consistent
altitude: Initiative/app (above the 5 epics)
updated: 2026-07-20T01:13
---

- (direction) Fast path, spine-only deliverable (build substrate for KseiPo + AI dev agents). Consolidation of existing docs, not from-scratch.
- (decision) Paradigm: Hexagonal (ports/adapters) over file-as-truth substrate; model contract pinned by shared golden fixtures across JS reference + Dart port. Binds all units; prevents domain/platform coupling and JS/Dart drift.
- (decision) AD-1 files single source of truth, derived never persisted. AD-2 model shape pinned by test/fixtures/lore-model, fixtures-first. AD-3 all FS via RepoStorage port. AD-4 writes atomic temp+rename, byte-exact UTF-8. AD-5 external syncer owns propagation, app never syncs/merges. AD-6 RU/EN pair a view, save to individual file. AD-7 one convention matcher for highlighter+linter. AD-8 parsing/highlighting total, never throws. AD-9 domain pure, I/O only in adapters behind ports, deps point inward. AD-10 model rebuilt not patched; editor owns buffer, loader owns model. AD-11 AI user-keyed HTTPS gated by context preview, key in secure storage, no telemetry. All [ADOPTED] from settled source docs.
- (assumption) Library versions NOT pinned (app unscaffolded) — Flutter stable channel + flutter_secure_storage current stable, resolved at flutter create/pub add. To verify at scaffold.
- (event) Spine drafted (Fast path) from PRD/addendum/epics/MOBILE/ARCHITECTURE/project-context.
- (decision) Packaging = feature-first vertical slices (flat peers: lore/, storage/, ai/), NOT layer-first. Per KseiPo. AD-9 reworded to per-slice file-level purity; added AD-12 (slice internals private, cross-slice only via ports/exports). Hexagonal rules unchanged, only packaging axis. Updated paradigm, diagram, tree, capability map.
- (decision) Reviewer gate (adversarial) closed 2 holes: AD-10 now blocks/save structural move on a dirty open file; AD-11 now says ai/ slice produces text only, never writes files (lore/ persists via RepoStorage) = single writer per file.
- (event) spine finalized
